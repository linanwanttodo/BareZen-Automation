name: github-star
runtime: node
entry: dist/index.js
description: Star or unstar a repository
inputs:
  repo: { required: true, type: string }
  action: { required: false, type: string, default: star }
outputs:
  result: { type: object }
