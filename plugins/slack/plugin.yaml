name: slack
runtime: node
entry: dist/index.js
description: Send a message to a Slack incoming webhook, splitting long text into parts
inputs:
  message: { required: true, type: string }
outputs:
  sent: { type: boolean }
  parts: { type: number, description: Number of messages sent after splitting }
